%ul#blog-posts.posts
  - all_articles.each_with_index do |article, i|
    %li
      %span.grey #{delocalized_date article.date} »
      =link_to article.title, article
      - article.tags.each do |tag|
        %div.pull-right
          %a{:href => tag_path(tag)}
            %span.label.label-info
              =tag
